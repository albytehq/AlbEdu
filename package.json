{
  "name": "albedu",
  "version": "v0.818.1",
  "description": "AlbEdu — Production-grade assessment platform for schools, campuses, and personal use. AI-maintainable by-feature structure.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/minify.mjs",
    "start": "node scripts/serve.mjs",
    "verify": "node scripts/verify-structure.mjs",
    "lint": "eslint src/ --ext .js 2>/dev/null || echo 'ESLint not configured — skipping'",
    "test": "echo 'Tests in tests/ directory — run manually' && exit 0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "jsdom": "^29.1.1",
    "lightningcss": "^1.25.0"
  },
  "keywords": [
    "education",
    "assessment",
    "supabase",
    "vanilla-js",
    "by-feature"
  ],
  "license": "MIT",
  "dependencies": {
    "actly": "^1.2.0"
  }
}
