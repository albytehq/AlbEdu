// src/pages/index.js — barrel of page controllers (for discoverability only).
// Page controllers are IIFEs that auto-init on DOMContentLoaded; they are
// loaded by their HTML pages via <script defer> tags, not via this barrel.

export const PAGES = [
    'create-assessment.js',
    'active-assessments.js',
    'results-analytics.js',
    'assessment-entry.js',
    'take-assessment.js',
    'daftar-nama.js',
    'panel.js',
];

export default PAGES;
